server:
  port: 8081  # Prod port

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/labdb
    username: root
    password: Shylaja@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
